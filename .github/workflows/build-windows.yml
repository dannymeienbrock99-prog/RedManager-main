name: Build CrazyBatto RedManager

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - name: Build temporarily paused
        run: echo "Automatic build is paused while the SOTF death-counter fix is applied."
