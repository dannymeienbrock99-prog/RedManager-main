name: Build CrazyBatto RedManager

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - name: Build temporarily paused
        run: echo "The final 1.2.2 installer workflow is being prepared."
